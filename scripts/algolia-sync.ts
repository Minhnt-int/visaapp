import { mockVisaPageData, mockTours, mockNews } from '../lib/mock-data';

console.log(`Visa records imported: ${mockVisaPageData.length}`);
console.log(`Tour records imported: ${mockTours.length}`);
console.log(`News records imported: ${mockNews.length}`);
